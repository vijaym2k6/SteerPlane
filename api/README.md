# SteerPlane API

FastAPI server for the SteerPlane control plane.

## Setup

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

Open http://localhost:8000/docs for API documentation.
